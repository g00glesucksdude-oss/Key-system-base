local url = "https://raw.githubusercontent.com/g00glesucksdude-oss/Complicated-key-system/main/b64"
local chunk = game:HttpGet(url)
print("Raw length:", #chunk)

local fn, err = loadstring(chunk)
if not fn then
    warn("Load error:", err)
    return
end

local result = fn()
print("Type of result:", typeof(result))
